# Python dependencies for Mini IT Platform API
# Install: pip install -r requirements.txt
# Note: JWT uses PyJWT (import jwt), not python-jose — not used in this codebase.

# Web framework
fastapi==0.136.1
uvicorn[standard]==0.47.0
python-multipart==0.0.29

# Database
sqlalchemy==2.0.49
psycopg2-binary==2.9.12

# Configuration
python-dotenv==1.2.2

# Auth
PyJWT==2.12.1
passlib[bcrypt]==1.7.4
# passlib 1.7.4 expects bcrypt.__about__.__version__ (removed in bcrypt 4.1+)
bcrypt==4.0.1

# Validation (EmailStr in schemas)
pydantic[email]==2.13.4

# Rate limiting
slowapi==0.1.9

# Cache
redis==5.2.1

# Migrations
alembic==1.14.1

# Testing (installed into the container so you can run pytest inside Docker)
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1
